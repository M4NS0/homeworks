# REPL Read, Evalute and Print Loop

# () or [] or {} makes thinks that is a function inside
# inside the quote is the argument inside parentesis

println("Hello, Julia!")

# Pressing Shift+Enter runs a cell of code, as jupyter notebook
# Strings in Julia are immutable
println(5+5)
println(5 - 3.2)
println(5 - 3.8 - 2.2, "....evaluted", "                lots of empty space")
println("Hello "^3)
println((" He"*"llo ")^5)
