### A Hello to TypeScript

```ts
function log(message) {
    console.log(message);
}
var message = "Hello World";
log(message);

```
#### Transpilating:
```sh
    sudo npm install -g typescript
    tsc *.ts && node main.js
```

